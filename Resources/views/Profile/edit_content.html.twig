{% trans_default_domain 'FOSUserBundle' %}

<div class="row">
    <div class="col-md-6">
        <form novalidate action="{{ path('fos_user_profile_edit') }}" {{ form_enctype(form) }} method="POST">
            {% form_theme form 'MsiAdminBundle:Form:form.html.twig' %}
            {{ form_widget(form) }}
            <input class="btn btn-default" type="submit" value="{{ 'group.edit.submit'|trans }}">
        </form>
    </div>
</div>
